/*
 * Host definitions for QNT9 services monitoring
 */

// Search Service
object Host "search-service" {
  import "generic-host"
  address = "search-service"
  vars.http_port = 8000
  vars.http_uri = "/api/v1/health"
  vars.notification["mail"] = {
    groups = [ "icingaadmins" ]
  }
}

// Data Ingestion Service
object Host "data-ingestion-service" {
  import "generic-host"
  address = "data-ingestion-service"
  vars.http_port = 8001
  vars.http_uri = "/health"
  vars.notification["mail"] = {
    groups = [ "icingaadmins" ]
  }
}

// ETL Pipeline Service
object Host "etl-pipeline-service" {
  import "generic-host"
  address = "etl-pipeline-service"
  vars.http_port = 8002
  vars.http_uri = "/health"
  vars.notification["mail"] = {
    groups = [ "icingaadmins" ]
  }
}

// Frontend Service
object Host "frontend-service" {
  import "generic-host"
  address = "frontend-service"
  vars.http_port = 8080
  vars.http_uri = "/health"
  vars.notification["mail"] = {
    groups = [ "icingaadmins" ]
  }
}

// PostgreSQL Database
object Host "postgres" {
  import "generic-host"
  address = "postgres"
  vars.pgsql_port = 5432
  vars.notification["mail"] = {
    groups = [ "icingaadmins" ]
  }
}

// TimescaleDB Database
object Host "timescaledb" {
  import "generic-host"
  address = "timescaledb"
  vars.pgsql_port = 5432
  vars.notification["mail"] = {
    groups = [ "icingaadmins" ]
  }
}

// Redis Cache
object Host "redis" {
  import "generic-host"
  address = "redis"
  vars.redis_port = 6379
  vars.notification["mail"] = {
    groups = [ "icingaadmins" ]
  }
}

// Kafka
object Host "kafka" {
  import "generic-host"
  address = "kafka"
  vars.tcp_port = 9092
  vars.notification["mail"] = {
    groups = [ "icingaadmins" ]
  }
}

// Consul
object Host "consul" {
  import "generic-host"
  address = "consul"
  vars.http_port = 8500
  vars.http_uri = "/v1/status/leader"
  vars.notification["mail"] = {
    groups = [ "icingaadmins" ]
  }
}

// Feature Engineering Service
object Host "feature-engineering-service" {
  import "generic-host"
  address = "feature-engineering-service"
  vars.http_port = 8003
  vars.http_uri = "/health"
  vars.notification["mail"] = {
    groups = [ "icingaadmins" ]
  }
}
