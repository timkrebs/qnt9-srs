# Grafana Dashboard Provisioning
apiVersion: 1

providers:
  - name: 'finio-dashboards'
    orgId: 1
    folder: 'finio'
    folderUid: 'finio'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
