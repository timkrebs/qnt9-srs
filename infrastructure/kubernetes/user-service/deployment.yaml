# User Service Kubernetes Deployment
# Environment: ${ENVIRONMENT}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: qnt9
  labels:
    app: user-service
    version: v1
    environment: ${ENVIRONMENT}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: user-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8020"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: qnt9-service-account
      imagePullSecrets:
        - name: acr-secret
      containers:
        - name: user-service
          image: ${ACR_LOGIN_SERVER}/qnt9-user-service:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 8020
              protocol: TCP
          env:
            - name: ENVIRONMENT
              value: "${ENVIRONMENT}"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: qnt9-secrets
                  key: database-url
            - name: AUTH_SERVICE_URL
              value: "http://auth-service:8010"
            - name: LOG_LEVEL
              value: "INFO"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8020
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8020
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - user-service
                topologyKey: kubernetes.io/hostname
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: user-service
